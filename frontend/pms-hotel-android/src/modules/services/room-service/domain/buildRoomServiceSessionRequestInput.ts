import { type AddSessionServiceRequestInput } from '@/modules/service-requests';
import { type RoomServiceCartState } from './models/RoomServiceCart'; import { type RoomServiceMenu } from './models/RoomServiceMenu';
export function buildRoomServiceSessionRequestInput({cart,deliveryTime,menu,serviceDate,summary}:{cart:RoomServiceCartState;deliveryTime:string;menu:RoomServiceMenu;serviceDate:string;summary:string}):AddSessionServiceRequestInput{const lines=cart.items.flatMap(line=>{const item=menu.items.find(candidate=>candidate.fixtureKey===line.itemFixtureKey);return item?[{label:item.name,quantity:line.quantity,priceText:`Q ${item.priceAmount}`}]:[]});const total=cart.items.reduce((value,line)=>{const item=menu.items.find(candidate=>candidate.fixtureKey===line.itemFixtureKey);return value+(item?item.priceAmount*line.quantity:0)},0);return{kind:'ROOM_SERVICE',origin:'SERVICES',status:'REQUESTED',title:'Room Service',summary,details:{type:'ROOM_SERVICE',serviceDate,items:cart.items,deliveryTime},billingSnapshot:{label:'Room Service',priceText:`Q ${total}`,lineItems:lines,amountMinor:total*100,currency:'GTQ',amountNature:'CONFIRMED'}}}
