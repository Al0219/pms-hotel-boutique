/** Frontend-only fixture. It is deliberately isolated from ReservationStay data. */
export const accessReservationFixture = {
  email: 'ana@example.com',
  reservationCode: 'HB-2026-004281',
} as const;
