import { type CheckoutFixtureDto, type FolioFixtureDto, type InvoiceFixtureDto } from '@/modules/checkout/data/dto/CheckoutFixtureDto';
import { type CheckoutContent, type Folio, type Invoice } from '@/modules/checkout/domain/Checkout';
function text(value: string, name: string) { if (!value.trim()) throw new Error(`${name} must not be blank`); return value; }
export function mapFolioFixtureDto(dto: FolioFixtureDto): Folio { return { totalStayText: text(dto.totalStayText, 'totalStayText'), paidGuaranteeText: text(dto.paidGuaranteeText, 'paidGuaranteeText'), pendingBalanceText: text(dto.pendingBalanceText, 'pendingBalanceText'), items: dto.items.map((item) => ({ key: text(item.fixtureKey, 'fixtureKey'), label: text(item.label, 'label'), priceText: text(item.priceText, 'priceText') })), totalText: text(dto.totalText, 'totalText') }; }
export function mapCheckoutFixtureDto(dto: CheckoutFixtureDto): CheckoutContent { return { roomDisplayText: text(dto.stay.roomDisplayText, 'roomDisplayText'), stayDatesText: text(dto.stay.stayDatesText, 'stayDatesText'), expectedDepartureText: text(dto.stay.expectedDepartureText, 'expectedDepartureText'), departureNoteText: dto.departureNoteText, checks: dto.checks.map((item) => ({ key: text(item.fixtureKey, 'fixtureKey'), label: text(item.label, 'label'), valueText: text(item.valueText, 'valueText') })) }; }
export function mapInvoiceFixtureDto(dto: InvoiceFixtureDto): Invoice { return { documentTypeText: text(dto.documentTypeText, 'documentTypeText'), referenceText: text(dto.referenceText, 'referenceText'), totalText: text(dto.totalText, 'totalText'), statusText: text(dto.statusText, 'statusText'), dateText: text(dto.dateText, 'dateText') }; }
