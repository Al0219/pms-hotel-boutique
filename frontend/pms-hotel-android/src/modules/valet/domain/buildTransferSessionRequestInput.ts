import { type AddSessionServiceRequestInput } from '@/modules/service-requests';
export function buildTransferSessionRequestInput({destinationKey,estimatedAmount,fareText,passengers,pickupKey,scheduledAtMs,summary}:{destinationKey:string;estimatedAmount:number;fareText:string;passengers:number;pickupKey?:string;scheduledAtMs:number;summary:string}):AddSessionServiceRequestInput{return{kind:'TRANSFER',origin:'VALET',status:'REQUESTED',title:'Traslado',summary,details:{type:'TRANSFER',destinationKey,...(pickupKey?{pickupKey}:{}),scheduledAtMs,passengers},billingSnapshot:{label:'Traslado · tarifa estimada',priceText:fareText,amountMinor:estimatedAmount*100,currency:'GTQ',amountNature:'ESTIMATED'}}}
