# Lib

Technical cross-cutting infrastructure belongs here. Environment configuration, HTTP transport, and technical errors may be added by their READY backlog tasks.

`lib` never depends on `src/modules` and contains no business logic or role-specific UI. This Structure Freeze adds no implementation here.
