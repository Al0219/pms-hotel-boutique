# Shared

Generic reusable code with collective ownership. It has no business rules and never depends on `src/modules`.

Visual shared components begin only with their first READY backlog task and demonstrated reuse. This Structure Freeze intentionally creates none.
